package se.kth.iv1350.exe3.contr.model;

/**
 * A placeholder fo some class in the model.
 */
public class ClassInModel {
    /**
     * Simulates some method that produces a result.
     * 
     * @param operand The number two which two will be added.
     * @return        <code>operand<code> + 2
     */
    public int addTwo(int operand) {
        return operand + 2;
    }
    
}
