package se.kth.iv1350.exe3.contr.integration;

/**
 * A placeholder for some class whose task is to read and write to a file.
 */
public class FileHandler {
    private String fileName;
    
    /**
     * Creates anew instance that uses the specified file.
     * 
     * @param fileName The file from which to read and write. 
     */
    public FileHandler(String fileName) {
        this.fileName = fileName;
    }
    
}
